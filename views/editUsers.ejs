<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile - Edit</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <style>
        /* Custom Red, Black, and White Theme */
        body {
            background-color: black;
            color: #cc0000;
        }

        h1, h4 {
            color: #cc0000; /* Red */
        }

        .card {
            background-color: #000; /* Black background */
            color: #fff; /* White text */
        }

        .card-body {
            background-color: #333; /* Dark grey background */
            border-radius: 8px;
        }

        .form-group label {
            color: #cc0000; /* Red */
        }

        .form-control {
            background-color: #222; /* Dark background */
            color: #fff; /* White text */
            border: 1px solid #cc0000; /* Red border */
        }

        .form-control:focus {
            background-color: #333; /* Dark grey when focused */
            border-color: #cc0000; /* Red border when focused */
            box-shadow: none; /* Remove shadow */
        }

        .btn-primary {
            background-color: #cc0000; /* Red button */
            border-color: #cc0000; /* Red border */
        }

        .btn-primary:hover {
            background-color: #cc0000; /* Darker red on hover */
            border-color: #c12e2a; /* Darker red border on hover */
        }

        ul {
            list-style: none;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Edit Profile: <%= users.user_id %></h1>

        <!-- Display User Details -->
<div class="card mb-4">
    <div class="card-body">
        <h4>User Information</h4>
        <ul>
            <li><b>Username:</b> <%= users.name %></li>
            <li><b>Email:</b> <%= users.email %></li>
            <li><b>Password:</b> <%= users.password %></li>
            <li><b>Created At:</b> <%= users.created_at %></li>
            <li><b>Updated At:</b> <%= users.updated_at %></li>
        </ul>
    </div>
</div>

<!-- Edit User Form -->
<form action="/users/update-user/<%= users.user_id %>" method="POST">
    <div class="form-group">
        <label for="name">Username</label>
        <input type="text" id="name" name="name" class="form-control" placeholder="Enter Username" value="<%= users.name %>" required>
    </div>
    <div class="form-group">
        <label for="email">Email</label>
        <input type="email" id="email" name="email" class="form-control" placeholder="Enter Email" value="<%= users.email %>" required>
    </div>
    <div class="form-group">
        <label for="password">Password</label>
        <input type="password" id="password" name="password" class="form-control" value="<%= users.password %>" readonly>
    </div>
    <button type="submit" class="btn btn-primary btn-block">Save Changes</button>
</form>


    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0Xn7RIF3kXov2+K7w0a+Y3hT6uY8URxx+VdCzXj7mVt8V+Xb" crossorigin="anonymous"></script>
</body>

</html>
