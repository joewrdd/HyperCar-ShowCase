<!DOCTYPE html>
<html lang="en">
<head>
    <!-- Meta tags for character set and responsive design -->
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Service Center Details - HyperCar Showcase</title>

    <!-- CSS styles for the page -->
    <style>
        /* General body styling */
        body {
            font-family: Arial, sans-serif;
            background-color: #1a1a1a; 
            color: #fff; 
            margin: 0;
            padding: 0;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        /* Header styling */
        header {
            background-color: #cc0000; 
            padding: 2.3rem 2rem;
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            z-index: 10;
            display: flex;
            align-items: center;
            box-shadow: 0 4px 6px rgba(0, 0, 0, 0.4);
        }

        /* Logo inside the header */
        header img {
            height: 100px;
            width: auto;
            position: absolute;
            top: 63%;
            transform: translateY(-50%);
            left: 20px;
        }

        /* Return Button */
        .back-button {
            position: absolute;
            top: 10px;
            left: 20px;
            text-decoration: none;
            font-size: 18px;
            color: #fff;
            background-color: #cc0000;
            padding: 10px 15px;
            border-radius: 6px;
            font-weight: bold;
        }

        .back-button:hover {
            background-color: #990000;
        }

        main {
            flex: 1;
            margin-top: 80px;
            padding: 0 2rem;
        }

        /* Service Center details section styling */
        .service-center-details {
            margin-top: 2rem;
            padding: 2rem;
            background-color: #333;
            border-radius: 8px;
            box-shadow: 0 0 15px rgba(0, 0, 0, 0.75);
        }

        .service-center-details h1 {
            color: #fff;
        }

        .service-center-details p {
            color: #ddd;
            font-size: 1.1rem;
            margin: 0.5rem 0;
        }

        /* Footer Styles */
        footer {
            text-align: center;
            padding: 1rem;
            background: #333;
            color: #ccc;
            margin-top: 2rem;
        }

        footer a {
            color: #ff0000;
            text-decoration: none;
        }

        footer a:hover {
            text-decoration: underline;
        }
    </style>
</head>

<body>
    <!-- Header containing the logo -->
    <header>
        <img src="/images/logo.png" alt="HyperCar Showcase Logo">
    </header>
    <main>
        <section class="service_centers">
            <h1>Service Centers</h1>
            <!-- Manufaturer Details In Rows Dynamically -->
            <% serviceCenters.forEach(serviceCenters => { %>
                <div class="service-center-details">
                    <h2><%= serviceCenters.name %></h2>
                    <p><strong>Location:</strong> <%= serviceCenters.location %></p>
                    <p><strong>Contact Number:</strong> <%= serviceCenters.contact_nb %></p>
                    <p><strong>Services Offered:</strong> <%= serviceCenters.services_offered %></p>
                </div>
            <% }); %>
        </section>
    </main>

    <!-- Footer Section with Back To Dashoard Return Route -->
    <footer>
        <p>&copy; 2024 HyperCar Showcase. All Rights Reserved <a href="dashboard">Back to Dashboard</a></p>
    </footer>
</body>

</html>
