<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User Profile - Edit</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous">
    <style>
        /* Custom Red, Black, and White Theme */
        body {
            background-color: black;
            color: #cc0000;
        }

        h1, h4 {
            color: #cc0000; /* Red */
        }

        .card {
            background-color: #000; /* Black background */
            color: #fff; /* White text */
        }

        .card-body {
            background-color: #333; /* Dark grey background */
            border-radius: 8px;
        }

        .form-group label {
            color: #cc0000; /* Red */
        }

        .form-control {
            background-color: #222; /* Dark background */
            color: #fff; /* White text */
            border: 1px solid #cc0000; /* Red border */
        }

        .form-control:focus {
            background-color: #333; /* Dark grey when focused */
            border-color: #cc0000; /* Red border when focused */
            box-shadow: none; /* Remove shadow */
        }

        .btn-primary {
            background-color: #cc0000; /* Red button */
            border-color: #cc0000; /* Red border */
        }

        .btn-primary:hover {
            background-color: #cc0000; /* Darker red on hover */
            border-color: #c12e2a; /* Darker red border on hover */
        }

        ul {
            list-style: none;
        }
    </style>
</head>

<body>
    <div class="container mt-5">
        <h1 class="text-center mb-4">Edit Profile: <%= review.review_id %></h1>

        <!-- Display user details -->
        <div class="card mb-4">
            <div class="card-body">
                <h4>Review Information</h4>
                <ul>
                    <li><b>User ID:</b> <%= review.user_id %></li>
                    <li><b>Car ID:</b> <%= review.car_id %></li>
                    <li><b>Rating:</b> <%= review.rating %></li>
                    <li><b>Comment:</b> <%= review.comment %></li>
                    <li><b>Date Posted:</b> <%= review.date_posted %></li>
                </ul>
            </div>
        </div>

        <!-- Edit User Form -->
        <form action="/reviews/update-review/<%= review.review_id %>" method="POST">
            <div class="form-group">
                <label for="car_id">Car ID</label>
                <input type="number" id="car_id" name="car_id" class="form-control" placeholder="Enter Car ID" value="<%= review.car_id %>" required>
            </div>
            <div class="form-group">
                <label for="user_id">User ID</label>
                <input type="number" id="user_id" name="user_id" class="form-control" placeholder="Enter User ID" value="<%= review.user_id %>" required>
            </div>
            <div class="form-group">
                <label for="date_posted">Date Posted</label>
                <input type="datetime" id="date_posted" name="date_posted" class="form-control" placeholder="Enter Date Posted" value="<%= review.date_posted %>" required>
            </div>
            
            <div class="form-group">
                <label for="rating">Rating</label>
                <input type="number" id="rating" name="rating" class="form-control" placeholder="Enter Rating" value="<%= review.rating %>" required>
            </div>
            <div class="form-group">
                <label for="comment">Comment</label>
                <input type="text" id="comment" name="comment" class="form-control" placeholder="Enter Comment" value="<%= review.comment %>" required>
            </div>

            <button type="submit" class="btn btn-primary btn-block">Save Changes</button>
        </form>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@4.3.1/dist/js/bootstrap.bundle.min.js" integrity="sha384-pzjw8f+ua7Kw1TIq0Xn7RIF3kXov2+K7w0a+Y3hT6uY8URxx+VdCzXj7mVt8V+Xb" crossorigin="anonymous"></script>
</body>

</html>
